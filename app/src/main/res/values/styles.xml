<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Bluetooth scan theme additions -->
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Fullscreen theme for splash/loading screens -->
    <style name="FullScreenTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- Bluetooth device list item style -->
    <style name="BluetoothDeviceItem" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">16dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>

    <!-- Bluetooth scan button style -->
    <style name="BluetoothButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/blue_500</item>
        <item name="cornerRadius">8dp</item>
    </style>
    <style name="ActiveTabButtonStyle">
        <item name="android:textColor">@color/green_700</item>
        <item name="android:background">@color/green_50</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="InactiveTabButtonStyle">
        <item name="android:textColor">@color/gray_600</item>
        <item name="android:background">@color/gray_100</item>
    </style>
</resources>